# Environment Variables Example for NUTRIA MACRO
# Copy this file to .env and fill in your actual values

# Supabase Configuration
# Use placeholders below and set real values in your local .env (do not commit secrets)
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=ey...your-anon-key...
# CRITICAL SECURITY: Service-role key must NEVER be exposed to the frontend
# Do NOT prefix with VITE_ - use only in secure server-side environments
# SUPABASE_SERVICE_ROLE_KEY=sk-service-role-key  # server-side only (Edge Functions/private backend)

# Invite Token System
# CRITICAL: This secret is used for HMAC-SHA256 hashing of invite tokens
# Generate a strong random secret (32+ characters) and NEVER commit the real value
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
INVITE_TOKEN_SECRET=your-secure-random-secret-here-32plus-chars

# Firecrawl MCP
FIRECRAWL_API_KEY=fc-your-api-key-here

# Google Gemini (optional - only if requested)
# GOOGLE_GEMINI_API_KEY=AIzaSy-your-gemini-api-key

# Other APIs (add as needed)
# OPENAI_API_KEY=sk-your-openai-key
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key
